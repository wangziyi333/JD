import React__default, { useState, useEffect } from "react";
import { createPortal } from "react-dom";
import { CSSTransition } from "react-transition-group";
import classNames from "classnames";
import { Close } from "@nutui/icons-react-taro";
import { View } from "@tarojs/components";
import { d as defaultOverlayProps, u as useLockScrollTaro, O as Overlay } from "./overlay.taro-C8eXSsJH.js";
import { C as ComponentDefaults } from "./typings-DV9RBfhj.js";
const defaultProps = Object.assign(Object.assign(Object.assign({}, ComponentDefaults), { position: "center", transition: "", overlayStyle: {}, overlayClassName: "", closeable: false, closeIconPosition: "top-right", closeIcon: "close", destroyOnClose: false, portal: null, overlay: true, round: false, onOpen: () => {
}, onClose: () => {
}, onOverlayClick: (e) => true, onCloseIconClick: (e) => true }), defaultOverlayProps);
const _zIndex = 1100;
const Popup = (props) => {
  const { children, visible, overlay, closeOnOverlayClick, overlayStyle, overlayClassName, zIndex, lockScroll, duration, closeable, closeIconPosition, closeIcon, left, title, description, style, transition, round, position, className, destroyOnClose, portal, onOpen, onClose, onOverlayClick, onCloseIconClick, afterShow, afterClose, onClick } = Object.assign(Object.assign({}, defaultProps), props);
  let innerIndex = zIndex || _zIndex;
  const [index, setIndex] = useState(innerIndex);
  const [innerVisible, setInnerVisible] = useState(visible);
  const [showChildren, setShowChildren] = useState(true);
  const [transitionName, setTransitionName] = useState("");
  const refObject = useLockScrollTaro(innerVisible && lockScroll);
  const classPrefix = "nut-popup";
  const baseStyle = {
    zIndex: index
  };
  const overlayStyles = Object.assign(Object.assign({}, overlayStyle), { "--nutui-overlay-zIndex": index });
  const popStyles = Object.assign(Object.assign({}, style), baseStyle);
  const popClassName = classNames({
    [`${classPrefix}`]: true,
    [`${classPrefix}-round`]: round || position === "bottom",
    [`${classPrefix}-${position}`]: true
  }, className);
  const closeClasses = classNames({
    [`${classPrefix}-title-right`]: true,
    [`${classPrefix}-title-right-${closeIconPosition}`]: true
  });
  const open = () => {
    if (!innerVisible) {
      setInnerVisible(true);
      setIndex(++innerIndex);
    }
    if (destroyOnClose) {
      setShowChildren(true);
    }
    onOpen && onOpen();
  };
  const close = () => {
    if (innerVisible) {
      setInnerVisible(false);
      if (destroyOnClose) {
        setTimeout(() => {
          setShowChildren(false);
        }, Number(duration));
      }
      onClose && onClose();
    }
  };
  const onHandleClickOverlay = (e) => {
    e.stopPropagation();
    if (closeOnOverlayClick) {
      const closed = onOverlayClick && onOverlayClick(e);
      closed && close();
    }
  };
  const onHandleClick = (e) => {
    onClick && onClick(e);
  };
  const onHandleClickCloseIcon = (e) => {
    const closed = onCloseIconClick && onCloseIconClick(e);
    closed && close();
  };
  const onHandleOpened = (e) => {
    afterShow && afterShow();
  };
  const onHandleClosed = (e) => {
    afterClose && afterClose();
  };
  const resolveContainer = (getContainer) => {
    const container = typeof getContainer === "function" ? getContainer() : getContainer;
    return container || document.body;
  };
  const renderToContainer = (getContainer, node) => {
    if (getContainer) {
      const container = resolveContainer(getContainer);
      return createPortal(node, container);
    }
    return node;
  };
  const renderTitle = () => {
    if (left || title || description) {
      return React__default.createElement(
        View,
        { className: `${classPrefix}-title` },
        position === "bottom" && React__default.createElement(
          React__default.Fragment,
          null,
          left && React__default.createElement(View, { className: `${classPrefix}-title-left` }, left),
          (title || description) && React__default.createElement(
            "div",
            { className: `${classPrefix}-title-title` },
            title,
            description && React__default.createElement("div", { className: `${classPrefix}-title-description` }, description)
          )
        ),
        closeable && React__default.createElement(View, { className: closeClasses, onClick: onHandleClickCloseIcon }, React__default.isValidElement(closeIcon) ? closeIcon : React__default.createElement(Close, null))
      );
    }
    if (closeable) {
      return React__default.createElement(React__default.Fragment, null, closeable && React__default.createElement(View, { className: closeClasses, onClick: onHandleClickCloseIcon }, React__default.isValidElement(closeIcon) ? closeIcon : React__default.createElement(Close, null)));
    }
  };
  const renderPop = () => {
    return React__default.createElement(
      CSSTransition,
      { nodeRef: refObject, classNames: transitionName, mountOnEnter: true, unmountOnExit: destroyOnClose, timeout: duration, in: innerVisible, onEntered: onHandleOpened, onExited: onHandleClosed },
      React__default.createElement(
        View,
        { ref: refObject, style: popStyles, className: popClassName, onClick: onHandleClick, catchMove: lockScroll },
        renderTitle(),
        showChildren ? children : ""
      )
    );
  };
  const renderNode = () => {
    return React__default.createElement(
      View,
      { catchMove: true },
      overlay ? React__default.createElement(Overlay, { style: overlayStyles, className: overlayClassName, visible: innerVisible, closeOnOverlayClick, lockScroll, duration, onClick: onHandleClickOverlay }) : null,
      renderPop()
    );
  };
  useEffect(() => {
    visible && open();
    !visible && close();
  }, [visible]);
  useEffect(() => {
    setTransitionName(transition || `${classPrefix}-slide-${position}`);
  }, [position, transition]);
  return React__default.createElement(React__default.Fragment, null, renderToContainer(portal, renderNode()));
};
Popup.displayName = "NutPopup";
export {
  Popup as P
};
