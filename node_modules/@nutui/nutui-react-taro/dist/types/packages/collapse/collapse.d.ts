import { FunctionComponent, ReactNode } from 'react';
import { BasicComponent } from '../../utils/typings';
import { default as CollapseItem } from '../collapseitem';
export type CollapseActiveName = Array<string> | string;
export interface CollapseProps extends BasicComponent {
    activeName: CollapseActiveName;
    defaultActiveName: CollapseActiveName;
    accordion: boolean;
    expandIcon: ReactNode;
    rotate: number;
    onChange: (activeName: CollapseActiveName, name: string, isOpen: boolean) => void;
}
export declare const Collapse: FunctionComponent<Partial<CollapseProps>> & {
    Item: typeof CollapseItem;
};
